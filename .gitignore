# Secrets / env
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
echo_radar.db
*.db
backend/_check*.py

# Node / Vite (frontend)
echo-app/node_modules
echo-app/dist
echo-app/.vite
node_modules/

# Misc
.DS_Store

# Telegram session files (session resolves to backend/radar/tg_session.session)
*.session
*.session-journal

# SQLite WAL/shared-memory runtime files
*.db-wal
*.db-shm

# Local tooling/agent dirs
.claude/
.understand-anything/

# Intel realtime subscription state (per-account runtime, not code)
backend/radar/intel/data/subscribed.json

# Media cache directory (runtime, not code)
backend/.media_cache/
